diff --git a/frontend/index.html b/frontend/index.html
index f87046b..6fc6a78 100644
--- a/frontend/index.html
+++ b/frontend/index.html
@@ -3,7 +3,7 @@
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
-    <title>Excalidraw POC - Backend API Integration</title>
+    <title>anet.chat</title>
     <style>
         body {
             margin: 0;
@@ -275,6 +275,45 @@
         .sync-time {
             color: #666;
         }
+
+        /* anet.chat: hide Excalidraw help-dialog top header
+           (Documentation / Blog / Found an issue? / YouTube) */
+        .HelpDialog__header { display: none !important; }
+
+        /* anet.chat: bake ShouShuTi (寿书体) as the CJK fallback font.
+           Trick: Excalidraw's bundled CJK font is named "Xiaolai" (constant
+           CJK_HAND_DRAWN_FALLBACK_FONT in @excalidraw/common). By declaring
+           our local font under the same family name, the browser's font
+           resolver picks ShouShuTi when Excalidraw asks for "Xiaolai" — no
+           bundle patching required. */
+        @font-face {
+            font-family: "Xiaolai";
+            src: url("/fonts/ShouShuTi.ttf") format("truetype");
+            font-weight: normal;
+            font-style: normal;
+            font-display: swap;
+            unicode-range: U+4E00-9FFF, U+3000-303F, U+FF00-FFEF, U+3400-4DBF, U+2E80-2EFF;
+        }
+        /* Keep "ShouShuTi" name available too for explicit references. */
+        @font-face {
+            font-family: "ShouShuTi";
+            src: url("/fonts/ShouShuTi.ttf") format("truetype");
+            font-display: swap;
+        }
+
+        /* Force the CJK font for any Chinese glyph in the canvas + editor.
+           Browser uses unicode-range to apply ShouShuTi only to CJK
+           characters, falling back to Excalidraw's hand-drawn fonts for
+           ASCII. */
+        .excalidraw,
+        .excalidraw .excalidraw-textEditorContainer,
+        .excalidraw .excalidraw__canvas,
+        .excalidraw textarea,
+        .excalidraw input,
+        .excalidraw .Stack {
+            font-family: "ShouShuTi", Virgil, Excalifont, "Cascadia",
+                         "Xiaolai", -apple-system, BlinkMacSystemFont, sans-serif;
+        }
     </style>
 </head>
 <body>
diff --git a/frontend/src/App.tsx b/frontend/src/App.tsx
index 9c328df..b03b7ca 100644
--- a/frontend/src/App.tsx
+++ b/frontend/src/App.tsx
@@ -1,12 +1,133 @@
 import React, { useState, useEffect, useRef } from 'react'
 import {
   Excalidraw,
+  MainMenu,
   convertToExcalidrawElements,
   CaptureUpdateAction,
   ExcalidrawImperativeAPI,
   exportToBlob,
   exportToSvg
 } from '@excalidraw/excalidraw'
+
+// anet.chat: force ShouShuTi for ALL canvas text including CJK fallback.
+//
+// 3-step defense in depth:
+//  (a) Register ShouShuTi as a FontFace named "ShouShuTi" (and also under
+//      Excalidraw's CJK fallback name "Xiaolai" — see CJK_HAND_DRAWN_FALLBACK_FONT
+//      in @excalidraw/common).
+//  (b) Hook CanvasRenderingContext2D.font setter — every assignment from
+//      Excalidraw includes "Xiaolai" in the font stack for CJK chars.
+//      We rewrite "Xiaolai" → "ShouShuTi" so the browser font resolver
+//      picks our @font-face whose src is ShouShuTi.ttf. This bypasses
+//      Excalidraw's hundreds of hashed Xiaolai-Regular-XXX.woff2 subsets.
+//  (c) Re-trigger Excalidraw refresh after our font loads so existing
+//      canvas elements re-render in the new font.
+//
+// Step (a) + (b) together is what actually makes it work — (a) alone wasn't
+// enough because Excalidraw subset-loads its own Xiaolai dynamically AFTER
+// our face register, sometimes winning the resolver race.
+;(function setupShouShuTi() {
+  try {
+    const FF = (window as any).FontFace
+    if (!FF || !document?.fonts) return
+    // Register explicit "ShouShuTi" face
+    const ff1 = new FF("ShouShuTi", "url(/fonts/ShouShuTi.ttf) format('truetype')",
+                      { display: "swap" })
+    ff1.load().then((loaded: FontFace) => {
+      document.fonts.add(loaded)
+      console.log("[anet] ShouShuTi font face added")
+    }).catch((e: unknown) => console.warn("[anet] ShouShuTi load failed:", e))
+
+    // Also register under "Xiaolai" with FULL CJK unicode-range as a safety net
+    const ff2 = new FF(
+      "Xiaolai",
+      "url(/fonts/ShouShuTi.ttf) format('truetype')",
+      {
+        unicodeRange: "U+4E00-9FFF, U+3000-303F, U+FF00-FFEF, U+3400-4DBF, U+2E80-2EFF",
+        display: "swap",
+      },
+    )
+    ff2.load().then((loaded: FontFace) => document.fonts.add(loaded)).catch(() => {})
+
+    // ── Step (b): hook ctx.font setter ────────────────────────────────
+    // Every <canvas> draws text via CanvasRenderingContext2D.font setter.
+    // We intercept and rewrite the font stack — replace any "Xiaolai" /
+    // 'Xiaolai' / "Virgil" reference with "ShouShuTi" so canvas rendering
+    // uses our shape data.
+    const proto = (window as any).CanvasRenderingContext2D?.prototype
+    if (proto && !proto.__anetFontHooked) {
+      const desc = Object.getOwnPropertyDescriptor(proto, "font")
+      if (desc && desc.set) {
+        const originalSet = desc.set
+        Object.defineProperty(proto, "font", {
+          configurable: true,
+          get: desc.get,
+          set(value: any) {
+            if (typeof value === "string") {
+              // Rewrite font family names. We use ShouShuTi for ALL handwritten
+              // and CJK rendering. Excalidraw's other fonts (Cascadia mono,
+              // Helvetica sans) are kept as-is.
+              value = value
+                .replace(/(['"]?)Xiaolai\1/g, "ShouShuTi")
+                .replace(/(['"]?)Virgil\1/g, "ShouShuTi")
+                .replace(/(['"]?)Excalifont\1/g, "ShouShuTi")
+            }
+            originalSet.call(this, value)
+          },
+        })
+        proto.__anetFontHooked = true
+        console.log("[anet] CanvasRenderingContext2D.font hook installed")
+      }
+    }
+  } catch (e) {
+    console.warn("[anet] font setup error:", e)
+  }
+})()
+
+// anet.chat: load pre-baked Excalidraw libraries from /libraries/*.excalidrawlib
+// The files are baked into the canvas Docker image (frontend/public/libraries/).
+// On mount, we fetch each one as a Blob and feed it to excalidrawAPI.updateLibrary().
+// `merge: true` so user-added libraries are not clobbered if the user later adds more.
+const PREBAKED_LIBRARIES = [
+  'software-architecture.excalidrawlib',
+  'aws-simple-icons.excalidrawlib',
+  'azure-cloud-services.excalidrawlib',
+  'information-architecture.excalidrawlib',
+  'lo-fi-wireframing-kit.excalidrawlib',
+  'stick-figures.excalidrawlib',
+]
+async function loadPreBakedLibraries(api: ExcalidrawImperativeAPI): Promise<void> {
+  for (const fname of PREBAKED_LIBRARIES) {
+    try {
+      const resp = await fetch(`/libraries/${fname}`)
+      if (!resp.ok) {
+        console.warn(`[anet] library ${fname}: HTTP ${resp.status}`)
+        continue
+      }
+      const blob = await resp.blob()
+      await api.updateLibrary({
+        libraryItems: blob,
+        merge: true,
+        openLibraryMenu: false,
+        defaultStatus: 'published',
+      })
+      console.log(`[anet] loaded library: ${fname}`)
+    } catch (e) {
+      console.warn(`[anet] library ${fname} failed:`, e)
+    }
+  }
+}
+
+// Inline website-globe icon for the "Join Agent Network" menu link
+const websiteIcon = (
+  <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor"
+       strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"
+       style={{ display: 'inline-block', verticalAlign: 'middle' }}>
+    <circle cx="12" cy="12" r="10" />
+    <line x1="2" y1="12" x2="22" y2="12" />
+    <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z" />
+  </svg>
+)
 import type { ExcalidrawElement, NonDeleted, NonDeletedExcalidrawElement } from '@excalidraw/excalidraw/types/element/types'
 import { convertMermaidToExcalidraw, DEFAULT_MERMAID_CONFIG } from './utils/mermaidConverter'
 import type { MermaidConfig } from '@excalidraw/mermaid-to-excalidraw'
@@ -347,6 +468,7 @@ function App(): JSX.Element {
   useEffect(() => {
     if (excalidrawAPI) {
       loadExistingElements()
+      loadPreBakedLibraries(excalidrawAPI)
 
       // Ensure WebSocket is connected for real-time updates
       if (!isConnected) {
@@ -837,6 +959,10 @@ function App(): JSX.Element {
   const clearCanvas = async (): Promise<void> => {
     if (excalidrawAPI) {
       try {
+        // anet.chat: use bulk clear endpoint — server preserves locked: true
+        // (the header name-plate). Falls back to per-element delete if the
+        // bulk endpoint fails.
+        await fetch('/api/elements/clear', { method: 'DELETE' })
         // Get all current elements and delete them from backend
         const response = await fetch('/api/elements')
         const result: ApiResponse = await response.json()
@@ -868,7 +994,7 @@ function App(): JSX.Element {
     <div className="app">
       {/* Header */}
       <div className="header">
-        <h1>Excalidraw Canvas</h1>
+        <h1>anet.chat</h1>
         <div className="controls">
           <div className="status">
             <div className={`status-dot ${isConnected ? 'status-connected' : 'status-disconnected'}`}></div>
@@ -929,7 +1055,28 @@ function App(): JSX.Element {
                 viewBackgroundColor: '#ffffff'
               }
             }}
-          />
+          >
+            <MainMenu>
+              <MainMenu.DefaultItems.LoadScene />
+              <MainMenu.DefaultItems.SaveToActiveFile />
+              <MainMenu.DefaultItems.Export />
+              <MainMenu.DefaultItems.SaveAsImage />
+              <MainMenu.DefaultItems.SearchMenu />
+              <MainMenu.DefaultItems.Help />
+              <MainMenu.DefaultItems.ClearCanvas />
+              <MainMenu.Separator />
+              <MainMenu.ItemLink
+                icon={websiteIcon}
+                href="https://agentnetwork.org.cn"
+                aria-label="Join Agent Network"
+              >
+                Join Agent Network
+              </MainMenu.ItemLink>
+              <MainMenu.Separator />
+              <MainMenu.DefaultItems.ToggleTheme />
+              <MainMenu.DefaultItems.ChangeCanvasBackground />
+            </MainMenu>
+          </Excalidraw>
         </div>
       </div>
     </div>
diff --git a/src/server.ts b/src/server.ts
index 8404b25..ff6d541 100644
--- a/src/server.ts
+++ b/src/server.ts
@@ -365,22 +365,36 @@ app.put('/api/elements/:id', (req: Request, res: Response) => {
 });
 
 // Clear all elements (must be before /:id route)
+// anet.chat patch: skip elements with locked:true so the name-plate header
+// survives both the custom "Clear Canvas" button and the mcp_excalidraw
+// clear_canvas MCP tool. Single-id DELETE is unaffected so the supervisor
+// can still individually delete header elements for repaint.
 app.delete('/api/elements/clear', (req: Request, res: Response) => {
   try {
-    const count = elements.size;
-    elements.clear();
+    let removed = 0;
+    let kept = 0;
+    for (const [id, el] of Array.from(elements.entries())) {
+      if ((el as any).locked === true) {
+        kept++;
+        continue;
+      }
+      elements.delete(id);
+      removed++;
+    }
 
     broadcast({
       type: 'canvas_cleared',
-      timestamp: new Date().toISOString()
-    });
+      timestamp: new Date().toISOString(),
+      preservedLockedCount: kept
+    } as any);
 
-    logger.info(`Canvas cleared: ${count} elements removed`);
+    logger.info(`Canvas cleared: ${removed} elements removed, ${kept} locked kept`);
 
     res.json({
       success: true,
-      message: `Cleared ${count} elements`,
-      count
+      message: `Cleared ${removed} elements (${kept} locked kept)`,
+      count: removed,
+      kept
     });
   } catch (error) {
     logger.error('Error clearing canvas:', error);
